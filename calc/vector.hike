package calc

type Vector struct {
	X int
	Y int
}

func (v *Vector) LengthSq() int {
	return (v.X * v.X) + (v.Y * v.Y)
}

func (v *Vector) Scale(factor int) {
	v.X *= factor
	v.Y *= factor
}
