package main

import "std/collections/list"

func printf(format string, ...) int

func main() int {
    var dummy list.List[int]
    listSize := sizeof(list.List[int])
    printf("LIST_SIZE_GE_16=%d\n", listSize >= 16)
    return 0
}