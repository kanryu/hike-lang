package main

import "std/map"

// 外部ランタイム関数
func printf(format string, ...) int

func main() int {
    m := make(map[string]int)
    m["Alice"] = 100
    m["Bob"] = 200

    if val, ok := m["Alice"]; ok {
        printf("Alice score: %d\n", val)
    }

    return 0
}