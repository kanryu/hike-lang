package main

import "std/fmt/sprintf"

var testOutputBuffer string = ""











func makeCounter(start int) func() int {
    c := start
    return func() int {
        c = c + 1
        return c
    }
}

func testOutput() string {
    cnt := makeCounter(10)
    v1 := cnt()
    v2 := cnt()
    testOutputBuffer += sprintf.Sprintf("CNT=%d,%d\n", v1, v2)
    return testOutputBuffer
}


func main() int { return 0 }
